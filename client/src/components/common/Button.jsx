import styled from "styled-components";

const Button = styled.button`
  padding: 5px;
  background: #7d48df;
  color: #fff;
  border: none;
  cursor: pointer;
  font-size: 0.8rem;
`;

export default Button;
